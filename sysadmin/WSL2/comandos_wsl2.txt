lista de distribuciones que estan andando
  wsl -l -v

instalar distro
    wsl --install -d Ubuntu

conectarse a una distro
  wsl -d Ubuntu

apagar distribucion
    wsl --terminate (distribucion)
    wsl --terminate kali-linux
