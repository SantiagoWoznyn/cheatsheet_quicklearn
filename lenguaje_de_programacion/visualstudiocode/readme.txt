lista de cosas a hacer en un principio en visual studio desktop:
INSTALAR NPM
NODE JS desde un exe

----------------------------------------------------------------------
aca va una lista de cosas a hacer en  visualstudio code server 

sudo apt update && sudo apt upgrade -y

instalar npm
  sudo apt install -y nodejs
  sudo apt install -y npm
--------------------------------------------------------------
tanto en desktop como en el server
Extensiones
python
dracula theme
astro

-------------------------
quizas un script para instalar todo esto mediante terminal :
